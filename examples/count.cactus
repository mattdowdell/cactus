fn main() {
	let start: i32 = 1;
	let limit: i32 = 10;
	let i: i32 = start;

	loop {
		if i >= limit {
			break;
		}

		print i;
		i += 1;
	}

	loop {
		if i < start {
			break;
		}

		print i;
		i -= 1;
	}
}
